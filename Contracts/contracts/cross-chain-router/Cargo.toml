[package]
name = "cross-chain-router"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0"
soroban-spec = "21.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
